name: Light Manager RDP

on:
push:
branches:
- main
workflow_dispatch:

jobs:
start-rdp:
runs-on: ubuntu-latest
steps:
- name: Checkout Repo
uses: actions/checkout@v3

- name: Setup RDP Script
run: |
echo "Starting Light Manager RDP..."
# यहाँ अपना RDP start script डालो
bash start-rdp.sh
